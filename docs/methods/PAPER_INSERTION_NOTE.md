# Paper insertion note

Insert `adaptive_soft_scaffold_methods.md` immediately after the existing
variable-increment/evolutionary-search subsection and before restrained
physical relaxation or statistical analysis.

The section is written as a complete Methods module. When merging it into the
main manuscript, preserve the definitions of RDKit Shape Tanimoto and Shape
Protrude distances established earlier in Methods and remove repeated
explanations only when the notation is already unambiguous.

The LaTeX equivalent is `adaptive_soft_scaffold_methods.tex`.
