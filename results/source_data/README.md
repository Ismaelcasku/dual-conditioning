# Figure source data

These files are the source tables used for the current manuscript figures.
They correspond to the frozen pre-re-audit analysis and are retained for exact
provenance.

A corrected campaign audit should write new files with a `_v2` suffix until the
manuscript and figures have been updated deliberately. Do not overwrite the
frozen source tables silently.
