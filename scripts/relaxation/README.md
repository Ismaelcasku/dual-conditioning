# Restrained relaxation

The exact OpenMM/PoseCheck scripts used during development are preserved under
`reproducibility/legacy_validation/`. They still depend on the original HPC
container and data layout. They should be parameterized and tested against the
archived ten-candidate panel before being promoted into the clean workflow.
