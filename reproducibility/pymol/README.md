# PyMOL reproduction scripts

Run these scripts from the repository root so their relative paths resolve.

```bash
make pymol-figure1-renderings
make pymol-figure3-renderings
make pymol-figure5-renderings
```

The `open_*.pml` files are interactive inspection helpers. Binary `.pse`
sessions are intentionally excluded from version control.
